/**
 * Here import all reducers and assign a key value into the combine reducer obj.
 * Example:
 * export default combineReducers({
 *  reducerKeyName: reducerName
 * });
 */
import { combineReducers } from 'redux';

export default combineReducers({});
